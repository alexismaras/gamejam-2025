using UnityEngine;

public class ClimbingObjectBehaviour : MonoBehaviour
{
    public Transform StartClimbTransform;
    public Transform EndClimbTransform;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
